<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header.ejs %>
  <style>
    table, th, td {
      padding: 5px;
      border: 1px solid black;
    }
    table {
      border-collapse: collapse;
      border-spacing: 0px;
    }
  </style>
</head>
<body>
    <p>Short URL - <a href="http://localhost:8080/u/<%= shortURL %>">http://localhost:8080/u/<%= shortURL %></a> | Visits: <%= visits %> | Unique Visitors: <%= uniques %></p>
    <form method="POST" action="/urls/<%= shortURL %>/delete"><input type="submit" value="delete"></form>
    <form method="POST" action="/urls/<%= shortURL %>/update"><input type="text" name="toBeUpdated" placeholder="http://"><input type="submit" value="update"></form>
    <table>
      <tr>
        <th>ID</th>
        <th>Time Accessed</th>
      </tr>
      <% for (let i = 0; i < time.length; i++) { %>
      <tr>
        <td><%= id[i] %></td>
        <td><%= time[i] %></td>
      </tr>
      <% } %>
    </table>
</body>
</html>